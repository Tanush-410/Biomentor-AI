import React from 'react'

export default function StemEducationBadge({ className = '' }) {
  return (
    <p className={`role-pill inline-flex w-fit border-zinc-300 bg-white/90 text-zinc-700 ${className}`}>
      STEM Education
    </p>
  )
}
